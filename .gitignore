# ── 依赖 ──
node_modules/
.pnp/
.pnp.js

# ── 构建产物 ──
dist/
build/
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages

# ── 环境变量 & 密钥 ──
.env
.env.local
.env.*.local
*.pem
*.key

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ── 日志 ──
*.log
logs/
npm-debug.log*

# ── 微信小程序 ──
project.private.config.json

# ── Flutter ──
pubspec.lock

# ── 上传目录 (用户上传的文件) ──
uploads/*
!uploads/.gitkeep

# ── 其他 ──
*.db
*.db-journal
coverage/
.cache/
